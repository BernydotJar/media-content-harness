'use client';
import Link from 'next/link';
import { useState, type FormEvent } from 'react';
import { api, useLoad, items, Header, Icon, Tag, Empty, Loading, Problem, Field, DateLabel, deviceLabels, type Item, type ApiError } from './ui';
export function Sources({ tenant, refresh }: { tenant: Item; refresh: () => void }) {
 const sources = useLoad('/tenants/' + tenant.tenant_id + '/sources'); const [uploading, setUploading] = useState(''); const [notice, setNotice] = useState(''); const [open, setOpen] = useState(false), [busy, setBusy] = useState(false), [error, setError] = useState<ApiError | null>(null);
 async function add(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); setBusy(true); setError(null); try { await api('/tenants/' + tenant.tenant_id + '/sources', { source: { id: f.get('id'), locator: f.get('locator'), purpose: f.get('purpose'), authorization: 'explicit', match: 'exact' } }); refresh(); sources.reload(); setOpen(false); } catch (err) { setError(err as ApiError); } finally { setBusy(false); } }
 const list = items(sources.data);
 async function upload(id: string, file?: File) { if (!file) return; setUploading(id); setError(null); try { if (file.size > 40 * 1024 * 1024) throw { code: 'BODY_TOO_LARGE', message: 'Selecciona un video de hasta 40 MB.' }; const response = await fetch('/api/v1/tenants/' + tenant.tenant_id + '/sources/' + id + '/upload', { method: 'POST', credentials: 'same-origin', headers: { 'Content-Type': file.type || 'video/mp4' }, body: file }); const result = await response.json(); if (!response.ok) throw result.error; sources.reload(); setNotice('Material subido y verificado. Ya está disponible para esta fuente.'); } catch (err) { setError(err as ApiError); } finally { setUploading(''); } }
 async function setMascot(sourceId: string) { setBusy(true); setError(null); try { await api('/tenants/' + tenant.tenant_id + '/mascot', { source_id: sourceId }); setNotice('Esta fuente quedó vinculada al personaje de tu marca. Selecciona una estrategia compatible al producir.'); refresh(); } catch (err) { setError(err as ApiError); } finally { setBusy(false); } }
 return <><Header eyebrow={tenant.organization + ' / BIBLIOTECA'} title="Tu material. Tus referencias." description="Define qué puedes usar y qué te inspira. Cada pieza conserva su propósito." action={<button className="primary" onClick={() => setOpen(v => !v)}>+ Agregar fuente</button>} /><div className="info-grid"><div className="panel mini-panel"><Icon name="sources" /><h3>Material de producción</h3><p>Fotos, videos y álbumes que tienes autorización para usar en tus historias.</p></div><div className="panel mini-panel"><Icon name="dna" /><h3>Referencias creativas</h3><p>Inspiran ritmo, narrativa y estilo. Su contenido no se reutiliza como material.</p></div></div>{open && <section className="panel form-panel"><h2>Agregar fuente o referencia</h2><form onSubmit={add}><div className="form-grid"><Field label="Nombre corto"><input name="id" required pattern="[A-Za-z0-9][A-Za-z0-9_-]{0,127}" placeholder="album-septiembre" /></Field><Field label="Propósito"><select name="purpose"><option value="source">Material de producción</option><option value="reference">Referencia creativa</option></select></Field></div><Field label="Enlace exacto"><input name="locator" type="url" required placeholder="https://…" /></Field><label className="check-line"><input type="checkbox" required />Confirmo que tengo autorización para acceder a este enlace con el propósito elegido.</label><p className="muted small-text">Agregar un enlace registra tu autorización. El material estará disponible para producir cuando la integración pueda acceder a sus archivos.</p><Problem error={error} /><div className="actions"><button className="primary" disabled={busy}>{busy ? 'Guardando…' : 'Guardar fuente'}</button><button type="button" className="secondary" onClick={() => setOpen(false)}>Cancelar</button></div></form></section>}{notice && <div className="notice success" role="status">{notice}</div>}<Problem error={error} />{sources.loading ? <Loading /> : sources.error ? <Problem error={sources.error} retry={sources.reload} /> : list.length ? <div className="source-grid">{list.map(s => <article className="panel source-card" key={s.id}><div className="source-visual"><Icon name={s.purpose === 'reference' ? 'dna' : 'sources'} /><Tag tone={s.purpose === 'reference' ? '' : 'warm'}>{s.purpose === 'reference' ? 'Referencia' : 'Material real'}</Tag></div><h3>{s.id}</h3><a href={s.locator} target="_blank" rel="noopener noreferrer">{s.locator}</a><div className="card-foot"><span>{s.inspection_status === 'UPLOADED' ? 'Material subido y verificado' : 'Enlace autorizado'}</span></div>{s.purpose === 'source' && <Field label={uploading === s.id ? 'Subiendo material…' : 'Subir video autorizado'} help="MP4, MOV o WebM. Máximo 40 MB."><input type="file" accept="video/mp4,video/quicktime,video/webm" disabled={!!uploading} onChange={e => { void upload(s.id, e.target.files?.[0]); e.target.value = ''; }} /></Field>}{s.purpose === 'source' && <button className="text-button" disabled={busy || tenant.brand?.mascot_asset_key === 'source:' + s.id} onClick={() => setMascot(s.id)}>{tenant.brand?.mascot_asset_key === 'source:' + s.id ? 'Fuente del personaje de marca' : 'Usar como fuente del personaje'}</button>}<details><summary>Autorización y procedencia</summary><dl className="source-proof"><dt>Propósito autorizado</dt><dd>{s.purpose === 'reference' ? 'Referencia creativa' : 'Material de producción'}</dd><dt>Alcance del acceso</dt><dd>Solo este enlace exacto</dd><dt>Última inspección del archivo</dt><dd>{s.media_asset ? <DateLabel value={s.media_asset.created_at || s.media_asset.uploaded_at} /> : 'No se ha inspeccionado material'}</dd>{s.media_asset && <><dt>Huella del archivo</dt><dd className="hash">{s.media_asset.sha256}</dd><dt>Formato y tamaño</dt><dd>{s.media_asset.mime_type} · {(s.media_asset.size_bytes / 1048576).toFixed(1)} MB</dd><dt>Procedencia</dt><dd>Archivo subido por una persona autorizada del espacio</dd></>}</dl></details></article>)}</div> : <Empty icon="sources" title="Trae algo de tu mundo.">Agrega tu primer álbum, video o referencia para empezar a construir historias.</Empty>}</>;
}
export function DNA({ tenant }: { tenant: Item }) {
 const resource = useLoad('/tenants/' + tenant.tenant_id + '/content-dna'), [open, setOpen] = useState(false), [busy, setBusy] = useState(false), [error, setError] = useState<ApiError | null>(null), [message, setMessage] = useState('');
 const references = (tenant.sources || []).filter((s: Item) => s.purpose === 'reference'), dna = resource.data?.dna || resource.data;
 async function analyze(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget), file = form.get('evidence') as File, ref = references.find((s: Item) => s.id === form.get('reference')); setBusy(true); setError(null); setMessage(''); try {
  if (!file?.size || !ref) throw { code: 'VALIDATION', message: 'Selecciona una referencia y un archivo de evidencia.' };
  if (file.size > 8 * 1024 * 1024) throw { code: 'VALIDATION', message: 'La evidencia debe pesar menos de 8 MB.' };
  if (!form.getAll('observed_elements').length) throw { code: 'VALIDATION', message: 'Selecciona al menos un elemento que hayas observado.' };
  const evidenceResponse = await fetch('/api/v1/tenants/' + tenant.tenant_id + '/sources/' + ref.id + '/evidence', { method: 'POST', credentials: 'same-origin', headers: { 'Content-Type': file.type || 'text/plain' }, body: file }); const evidenceResult = await evidenceResponse.json(); if (!evidenceResponse.ok) throw evidenceResult.error; const hash = evidenceResult.data.sha256;
  await api('/tenants/' + tenant.tenant_id + '/content-dna/analyze', { observations: [{ schema_version: 'reference-observation.v1', id: 'observation-' + crypto.randomUUID(), tenant_id: tenant.tenant_id, authorization_id: ref.id, locator: ref.locator, evidence_sha256: hash, format: form.get('format'), pacing: form.get('pacing'), branding: form.get('branding'), observed_elements: form.getAll('observed_elements') }] }); resource.reload(); setOpen(false); setMessage('Content DNA actualizado a partir de tu observación. La evidencia quedó registrada en tu espacio.');
 } catch (err) { setError(err as ApiError); } finally { setBusy(false); } }
 const elements: Record<string, string> = { 'brand-made-by-hand': 'Marca creada a mano', 'mascot-character': 'Personaje o mascota', 'walking-observation': 'Recorrido por el lugar', 'community-activity': 'Actividad de comunidad', 'place-detail': 'Detalles del lugar', 'human-closeup': 'Primeros planos humanos', 'object-closeup': 'Detalles de objetos', 'documentary-footage': 'Material documental', 'stylized-animation': 'Animación', 'ambient-sound': 'Sonido ambiente', 'minimal-on-screen-text': 'Poco texto en pantalla' };
 return <><Header eyebrow={tenant.organization + ' / IDENTIDAD CREATIVA'} title="Una voz que se reconoce." description="Content DNA transforma observaciones de tus referencias en recursos narrativos propios." action={<button className="primary" disabled={!references.length} onClick={() => setOpen(v => !v)}>+ Observar referencia</button>} />{message && <p className="notice success" role="status">{message}</p>}{!references.length && <div className="notice"><p>Primero agrega una referencia creativa autorizada.</p><Link href={'/workspace/' + tenant.tenant_id + '/sources'}>Ir a fuentes y referencias →</Link></div>}{open && <section className="panel form-panel"><h2>¿Qué hace funcionar esta referencia?</h2><p className="muted">Registra lo que observaste. El análisis abstrae recursos narrativos; no descarga ni copia el contenido.</p><form onSubmit={analyze}><Field label="Referencia"><select name="reference" required>{references.map((s: Item) => <option key={s.id} value={s.id}>{s.id}</option>)}</select></Field><Field label="Archivo de evidencia" help="Selecciona la captura o el archivo que respalda tu observación (máximo 8 MB). El archivo se guarda como evidencia de esta observación."><input name="evidence" type="file" accept="image/png,image/jpeg,image/webp,text/plain,application/json,video/mp4" required /></Field><div className="form-grid three"><Field label="Formato"><select name="format"><option value="reel">Reel</option><option value="video">Video</option><option value="post">Publicación</option><option value="image">Imagen</option><option value="mixed">Mixto</option></select></Field><Field label="Ritmo"><select name="pacing"><option value="moderate">Moderado</option><option value="quiet">Pausado</option><option value="fast">Rápido</option></select></Field><Field label="Presencia de marca"><select name="branding"><option value="subtle">Sutil</option><option value="moderate">Moderada</option><option value="prominent">Protagonista</option></select></Field></div><fieldset><legend>Elementos observados</legend><div className="check-grid">{Object.entries(elements).map(([value, label]) => <label className="check-line" key={value}><input type="checkbox" name="observed_elements" value={value} />{label}</label>)}</div></fieldset><Problem error={error} /><button className="primary" disabled={busy}>{busy ? 'Analizando…' : 'Construir Content DNA'}</button></form></section>}{resource.loading ? <Loading /> : resource.error ? <Problem error={resource.error} retry={resource.reload} /> : dna ? <div className="dna-layout"><section className="panel dna-main"><div className="dna-heading"><div className="empty-icon"><Icon name="dna" /></div><Tag tone="success">Identidad disponible</Tag></div><h2>Tu repertorio narrativo</h2><div className="dna-devices">{(dna.story_devices || []).map((d: string, i: number) => <div key={d}><span>{String(i + 1).padStart(2, '0')}</span><h3>{deviceLabels[d] || d.replaceAll('-', ' ')}</h3></div>)}</div></section><aside className="panel"><p className="eyebrow">LOS MATICES</p><h3>Ritmo</h3><p>{(dna.pacing_profile || []).map((v: string) => ({ quiet: 'Pausado', moderate: 'Moderado', fast: 'Rápido' }[v] || v)).join(', ') || 'Disponible en la observación'}</p><h3>Marca</h3><p>{(dna.branding_profile || []).map((v: string) => ({ subtle: 'Sutil', moderate: 'Moderada', prominent: 'Protagonista' }[v] || v)).join(', ') || 'Disponible en la observación'}</p><hr /><p className="muted">Ideas narrativas reutilizables. Sin copiar textos, planos o audio de otras personas.</p><Link className="button secondary" href={'/workspace/' + tenant.tenant_id + '/weekly'}>Usar en mi semana<Icon name="arrow" /></Link></aside></div> : <Empty icon="dna" title="Encuentra el hilo que une tus historias.">Observa una referencia para descubrir recursos de ritmo, marca y narrativa que puedas hacer tuyos.</Empty>}</>;
}
