# WEB054 — Creation Focus Surface V19 Release

## Exact product candidate

`859586d3bd7c65443c43dd7034f3688519611913`

This release node may deploy only that product commit. Evidence-only commits on the working branch are not deployment candidates.

## Release objective

Publish the verified V19 presentation-layer refinement to the existing controlled single-operator Media Factory preview, then prove that the public service and the authorized FIRMES workspace expose the reviewed Create hierarchy without weakening existing policy.

## Required pre-deploy evidence

- WEB053 is DONE on the exact product SHA.
- Production build is bound to the exact product SHA.
- Full suite, V19/V17 focused tests, TypeScript, contracts, dependency audit, and packaged browser E2E pass.
- IBM Granite 3.3 2B returns a PASS risk decision and all ten enterprise rubric categories PASS for the exact product SHA.
- The canonical signed-receipt finalizer verifies those raw model responses.
- The release archive is an immutable Git archive of the exact product SHA.

## Deployment boundary

Use the existing `media-factory` product registration and host reconciler. Do not change:

- hostname, tunnel, DNS, Caddy, edge topology, or deployment class;
- authentication, tenant isolation, authorization, or municipal access rules;
- provider-spend approval or Q200 accounting policy;
- publication behavior or content-policy behavior.

## Post-deploy acceptance

The host reconciler must converge with public health and process recovery PASS. Then the authorized Mac Chrome bridge must confirm on the FIRMES/Caballito Create surface:

- V19 focus surface is present;
- the dominant heading is “¿Qué quieres crear?”;
- the Q200 weekly allowance is visible in compact form;
- usage details are progressively disclosed;
- the main composer precedes the character/place shortcut;
- the prompt composer remains usable.

The bridge may observe only the authorized product surface. It must not inspect cookies, credentials, storage, private messages, unrelated tabs, or account profile data.

## Terminal condition

WEB054 is DONE only after exact signed deployment, host convergence, public health, and bounded authenticated bridge acceptance are all evidenced.
