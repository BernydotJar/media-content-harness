# User-directed evidence reuse checkpoint

The user instructed on 2026-09-12: finish, and do not repeat tests already performed. No new feature work remains in this candidate. Preserve passing evidence, verify unchanged source identities, and execute only the as-yet unverified build/runtime delta and publication gates.

The last integrated production-runtime browser walkthrough passed 33 checks at `/tmp/media-factory-browser-e2e-iRYEhl`, build `eO9WwJNfynweo0veAMHCu`, production source SHA256 `1ea358cb50aa95d25eab40064df4621c7abc5c3aa0c437c8757b56490a90e3f1`. It was explicitly a dirty-tree diagnostic with candidate_sha null. Its original result and full source-file manifest are retained in WEB002-addendum. It is not relabeled as an immutable-SHA test run.

`reuse-source-delta.json` compares every production-source hash in that executed build with the frozen candidate sources. The seven added/removed/changed paths are exclusively the static social-image route/import, controlled OG asset metadata and three corrected text colors. Authentication, APIs, tenant isolation, media execution, provenance, immutable releases, byte ranges, navigation, motion behavior and dependency files are unchanged from that passing walkthrough. The final source digest is `fe96cbc3e1e21ef01db97d59aef15e2debe050b1eb69763da66b6c64ca5a9ec1`.

Reuse the 89-test/13-contract baseline from commit83d7783 with the focused subsequent reports: 26 worker/projection tests, provider capability check, six HTTP range tests, seven metadata tests, the 33 browser checks, and the independently reviewed 31 controller/promotion tests. These are overlapping evidence sets, not a fabricated summed count or a claim of a fresh full-suite run. New dependencies already passed frozen installation and production audit in the creative-navigation producer checkpoint; lockfile identities are preserved there and in the passing build manifest.

Remaining delta: compile the frozen clean candidate once, confirm actual compiled public OG and generator bytes, confirm corrected text contrast, and independently audit the prepared runtime used for that delta. Then perform the required actual model review/signature and host deployment verification. Public deployment has never passed, so those are new gates, not repetition. Keep exact candidate SHA, reused evidence source identities, delta evidence, and eventual deployed SHA distinct.
