# WEB011 revision 1 — user acceptance repair scope

Authority: explicit user rejection and Graph Engineering continuation request on 2026-09-12 (America/Guatemala).

The prior completion claim is invalidated by live acceptance evidence. The repaired product must satisfy all of these before WEB011/descendants can close again:

1. FIRMES identity is actually active for the user's live Antigua workspace, including burgundy `#8E2C2D` as primary and gold `#BD9B60` as supporting accent. Do not infer brand from the organization display name alone.
2. The authorized FIRMES caballito/horse is visibly represented in the intended creation experience using an exact user-authorized manual-derived asset or another explicitly authorized asset; do not relabel a generic monogram as the official horse.
3. A real non-test video is generated by the Media Factory product workflow from authorized source bytes, with a playable artifact hash and persisted job evidence. Offline video preparation alone does not satisfy this gate.
4. The authenticated Chrome bridge is used as a read-only verifier (`inspect-main-content` / `capture-main-evidence`) against the live product. Product mutations continue through approved application/API/deployment paths; the bridge is not weakened into an unrestricted browser robot.
5. Every observed blocking condition has an actionable repair. The exact live job `mf_76e0f9795197d32b4c854721de7833d3` currently blocks on `SOURCE_TOO_SHORT` (visible message: “Uploaded footage is too short for the requested duration”) and exposes only “Volver a comprobar recursos”. That dead end must be repaired.
6. Final completion requires an exact deployed SHA, post-deploy authenticated bridge evidence, and a consultant-style walkthrough showing brand, caballito, creation/production, artifact preview, and repair navigation.

Pre-fix bridge evidence:
- `progress/media-factory-web/web011-r1/pre-fix-live-job.json`
- `progress/media-factory-web/web011-r1/pre-fix-live-job.png`

The bridge evidence is read-only and does not contain cookies, storage state, passwords, or websocket debugger URLs.
